using UnityEngine;

/// <summary>
/// Marker component to exclude a GameObject from automatic collider generation.
/// Add this to spawn points, special geometry, or anything that shouldn't be processed.
/// </summary>
public class ExcludeFromColliderGeneration : MonoBehaviour
{
    // This is just a marker - no logic needed
}
